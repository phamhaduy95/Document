
Docker stores all the data relating to your containers and images under a folder. 
can I move this directory to different place

docker perform all its internal interaction via RestFUL API

exposing port to outside in order to allow communication between container
horizontal scaling is impossible with docker

docker daemon 
