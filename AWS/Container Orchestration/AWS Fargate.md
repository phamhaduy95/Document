AWS Fargate is a serverless compute engine for containers that integrates seamlessly with both Amazon **Elastic Container Service** (**ECS**) and Amazon **Elastic Kubernetes Service** (**EKS**). Being a fully managed service, Fargate removes the operational overhead of managing underlying server infrastructure. Instead, users specify the CPU and memory requirements for their containers, and Fargate handles the provisioning and management of the compute resources.