
Similar to notifications, _repository triggers_ can invoke SNS or Lambda during these events:

- Push to Existing Branch
- Create a Branch or Tag
- Delete a Branch or Tag

Triggers are similar in functionality to webhooks used by other Git providers like GitHub. You can use triggers to perform automated tasks such as starting external builds, notifying administrators of code pushes, or performing unit tests.