Monitor and finetune TS performance time.

     Read this article of Microsoft ‘s typescript team to explore more about technique a

[Performance · microsoft/TypeScript Wiki · GitHub](https://github.com/microsoft/Typescript/wiki/Performance)

In short, prefer using interface when you have to compose multiple types together. However, for standalone declaration, both type and interface yield the same performance result.

Acquire knowledge and understanding about vite bundling tool.

How can we create a plugin that refining

I need to understand some termination of vite config

 Chunk: a unit of files that generated by vite rollup

Which file format is built first 😊

export {default as Component} from “./component” will make default import as regular module

So when you import in otherfile, instead of import Component from “.”, we have import {Component}

Many interactive UI components from ARK UI have notation “use client” on top of their files. As result, they will be rendered on client side and only useable on Client component in NextJs router app. Need a solution to bypass it or at least enable some parts to be prerendered from the server.

There some common code that several entries are depending in, it will be put in a chunk.

Write config file that contain the list of Components that is will be included in the output package.

Config file format can be JSON or TS file.

all index file will be included by default. The only kind of files that is needed to be filtered is component definition file since some files are used as dependencies but not a usable component.

Since it requires some extra steps to extract data from JSON file, I opt to JS file instead.

the structure of the config file:

{

            Includes: [ ‘Component1’, ‘Component2’, ‘Component3’, ‘Component4’]

}

Problem:

I rely on tsc to generate the type declaration for my project. However, tsc will try to generate d.ts file for all dependencies that it can find from the entries file ☹.

It seems that tailwind will try to gather all reachable css files in your directory and bundle those. So how can I configure the tailwind only include files that is imported.

[**Verdaccio**](https://verdaccio.org/) is a tool that allow you to publish your npm package privately.

Step to publish your package to npm.