statements
1. condition statement (if else)
2. loop statement (for loop, while loop)

operation & operator
1. logic (comparison, equal, greater, less)
2. arithmetic (addition, deduction or subtraction, multiplication, division, exponentiation)
3. bit operation (AND, OR, XOR, shifting)

variables 
1. declaration to declare new variable
2. value assignment 

primitive type
2. numeric (integer, float)
3. character
4. string: sequence of characters
5. bytes

data collection	
1. array  for single-typed (homogeneous) value
	1. fast random access via index
	2. fixed or dynamic size
	3. take a slice of array/ extract subarray
	4. add/remove element
	5. in place

2. dictionary (key-value pair table) 
	1. 

allocation

module
1. import and export
2. isolation
3. independency/decoupling

method / function
1. function signature:
	- method name
	- parameter consist of returned value and input argument
	- accept input 
	- passing arguments
	- returned value 
2. local variable
3. function scope
	1. nested function	
4. anonymous method (lambda)
5. high order function
6.  closure 

type annotation/type definition
1. generic type
2. literal type


substitute 

class
1. class is a blueprint or template to define properties and behaviors of objects from class
2. class instance (object)
3. instantiate/initiate an object
4. class methods is bound to class itself
5. class attributes include fields and methods defined within a class
6. class definition

list enumeration 
1. iterate over/loop though / list elements


in progress/ ongoing
incoming 