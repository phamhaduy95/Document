# Immutable Collections in C#

Immutable collections are collections that cannot be modified once they are created. Any operation that would change the collection (like adding or removing an item) instead returns a new collection with the changes applied, leaving the original collection intact.

These are found in the `System.Collections.Immutable` namespace.
