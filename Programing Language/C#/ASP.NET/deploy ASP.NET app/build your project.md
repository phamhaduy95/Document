build

dotnet run

dotnet publish \--output publishApp \--configuration Release

Always use the release configuration when publishing your app for\
deployment. This ensures the compiler generates optimized code for your
app.

dotnet
