What is t
with priority, it is guaranteed to return a maxim of the collection at a time. The priority queue is data structure often found to store in order for greedy algorithm.


heapq transform input list 
control the ordering


heapify_max 


