we can import story into test file that might use `Jest`, `Playwright` or `Cypress`. Doing so allows context reuse and instead focus on write code for simulating user interaction and assertion.

Question
Step to 

│  Note: both in 3.2 or with the ignore, the properties in the base type are
│  treated as fallthrough attrs at runtime.

│  ./lib/components/SingleSelect/SingleSelect.vue
│  7  |
│  8  |         export interface SingleSelectBaseProps
│  9  |                 extends Omit<SelectBaseProps, 'modelValue' | 'defaultValue' | 'multiple'>
│  {
│  |            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
│  10 |                 modelValue?: string;
│  11 |                 value?: string;


		default value of Boolean prop is false and not `undefined` like I expected


effective error boundary component using the Composition API and the built-in `onErrorCaptured` lifecycle hook.

problem we should cover
large file uploading:
- create a hidden form tag using `createElement` API
-  set Method to get
- set `href` attribute value with the `url` of download link
- trigger `submit()` 


large file downloading

testing

data queue with `vue`  

accept 
prepare architecture 
story 
data flow between backend and frontend.
need discussion about 
SQL query
on-premise 
tieu chi design
work 24/7, kho
login
design ban dau. 
kinh nghiem ve he thong cu.
sync session between tabs
redis caching
kinh nghiem testin
 question for write SQL query
authentication tool
encryption



real-time update dung share worker 
each tab open its own websocket it would consume lots of resource from both BE and FE 
