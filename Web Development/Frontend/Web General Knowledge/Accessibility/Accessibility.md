keyboard navigation support

aria-* tag and role 

color and size for 

Form 
every form field must contain the `<label>` tag 
ensure user can navigate through from field via keyboard

`tabindex= -1 
make browser wont provide auto focus 

how to ensure accessibility for table component

Having just basic level of accessibility for website would help testing a lot.
Since many UI testing frameworks assume its app under test  develop their API 

This section explain

The three main features of ARIA are roles, properties, and states/values.

If user 

Keyboard binding 
allow to execute
register certain command or action with 
need to double check if the combination is taken. In case of duplicate, should query user confirmation via popup.

you can not pass children as Component from Astro into React Component by default. However, you can turn on the passingReactChildrenExperience 


I just want to become the best person in this world. How can I achieve this 
you should turn off grammar checker in your text editor, which would help develop your awareness for grammar mistakes to avoid them in future. 

by default, markdown only support a small subset of HTML tags. To extend amount of tags supported, you can install Github flavored markdown (GFM) 


TaskList 
