next/ image

which aspect of image optimization does next image component provides

- Lazy loading: only fetch image when it reaches the certain part of your viewport
- Image size optimization: automatically resize image so that it can match any size of
- Prevent cumulate layout shifting by creating a fixed-size box where image will be loaded into.
- Caching image in next server.

This is automatically done in bundle building phase.

Security configuration settings:

Allowing fetching image from a small list of image source prevent hacker from injecting a malicious source of image.

server side rendering vs static site generation.

prebuild HTML sematic at build time.

To add interactivity for your site, the hydration process take place.