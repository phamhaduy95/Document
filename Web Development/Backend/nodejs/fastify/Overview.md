introduction
`Fastify`  là back-end framework chạy trên runtime `node.js`.
fastify được phát triển với các tiêu chỉ sau: 
-performance: nhanh hơn express từ 3 đến 4 lần
simple and straight-forward

Các component căn bản của Fastify bao gồm
khai báo controller + schema trong routes
hooks : cho phép khai báo 1 action trước hoặc sau 1 quá trình.
plugins: 

hiểu về plugins architect

lý dó tồn tại của decorator

decorators
	Decorating core objects with this API allows the underlying JavaScript engine to optimize the handling of server, request, and reply objects. This is accomplished by defining the shape of all such object instances before they are instantiated and used.

http life cycle

code structure


schema
controller
routes


**stream interface** that allows you to process large result sets without loading the entire dataset into memory at once. This is especially useful for handling a high volume of data in Node.js environments, where memory management is critica
